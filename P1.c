#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[])
{
	int V

		prinft("Valor: ")
			scanf(V) if (V >= 150)
				printf("Superior") if (V >= 50V <= 100)
					Printf("M�dio") if (V >= 35V <= 48)
						Printf("Inferior") return 0;
}
